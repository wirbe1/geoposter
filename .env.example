# Optional replacements for public OSM providers (must support browser CORS).
# For a production deployment, use providers sized for your traffic or a
# same-origin proxy with shared caching and rate limiting.
VITE_GEOCODER_URL=https://nominatim.openstreetmap.org/search
VITE_OVERPASS_URL=https://overpass-api.de/api/interpreter
